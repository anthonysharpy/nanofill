graph axis is kind of wrong - should start at 0?

when we take ownership of a new pool, we should ask the CPU to load it into cache. maybe

add needs main thread attention flag to pool -> pool will check then check what it needs to do
it will either grow, prefetch a just-growed thing

spawning pools stress test

create memory pools inside a memory pool for better locality

don't push pools to the queue. push the actual "next" pointer. so we share less cache

std::uint8_t events_found = 0; - probably faster as a larger type

don't give markets a fixed size?

event type needs to know what market it's for.
split event into two types -> incoming events and event

dont pin to core 0 because it's the core the os uses

kind of think all the nanofill:: prefixes are a bit unnecessary, remove?

check if we can use bit masks anywhere in if conditions

LevelPriceData::price is pointless to store because it's already known

OrderBook::reset_market may cause a use after free because the allocator thread might still access it.
Currently it's fine though because that function is only used for tests